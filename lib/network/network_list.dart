import 'package:flutter/material.dart';

class NetworkList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text('Network List'),
      ),
      body: Container(
        color: Colors.orange,
      ),
    );
  }
}