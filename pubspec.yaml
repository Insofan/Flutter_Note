name: flutter_note
description: Flutter note.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 依赖库
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 依赖库
  pedantic: ^1.0.0

flutter:

  uses-material-design: true

